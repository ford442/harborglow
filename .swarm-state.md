## kimi-cli swarm state — 2026-05-31T18:22:00+02:00
### Iteration 1
- Files changed: `src/scenes/ProceduralShip.tsx`
- Build: PASS (tsc + vite build succeed; `build:wasm` fails due to missing emsdk in env, not code-related)
- Lint: PASS (0 errors, 0 new warnings)
- What was done this iteration: Replaced the monochrome `Lod2Impostor` with per-feature-type colors, profile-driven hull tinting (`tintHullColor` via THREE.Color), and geometry variety (cylinderGeometry for funnel/tank/mast). Added `FEATURE_COLORS` lookup and `GLOWING_FEATURES` set to switch between `meshStandardMaterial` (emissive) and `meshLambertMaterial`.
- What remains: None — task complete.
- Blockers: none

### Verification Trace
- **cruise** (stepped): hull lightened ×1.15; superstructure `#c8d8e8` (Lambert), funnel `#222222` (Standard, emissive 0.3, cylinder).
- **lng** (tall): hull darkened ×0.85; tank `#d0dde8` (Standard, emissive 0.3, cylinder).
- **trawler** (tall): hull darkened ×0.85; mast `#1a1a1a` (Lambert, cylinder), gantry `#334455` (Lambert, box).

---

## kimi-cli swarm state — 2026-06-15T02:02:00+02:00
### Iteration 2 — Per-Ship LightCue Schedules
- Files changed:
  - Created `src/systems/lightShows/types.ts`
  - Created `src/systems/lightShows/lng.ts`
  - Created `src/systems/lightShows/tanker.ts`
  - Created `src/systems/lightShows/index.ts`
  - Modified `src/systems/lightingSystem.ts`
- Build: PASS (`npm run build` — tsc + vite build + build:wasm all green)
- Tests: PASS (`npm test` — 95/95 Vitest tests green)
- Lint: PASS (`npm run lint` — 0 errors; 26 pre-existing warnings, none in changed files)
- What was done this iteration:
  - Defined `LightCue` type with beat-locked `{ beat, pattern, color, intensity }` and pattern union (`breathe` | `sweep` | `strobe` | `snap` | `blackout`).
  - Authored `lngLightShow` (118 BPM, Ambient/Cryogenic Techno): slow cyan breathing, frost-blue sweeps, white/frost strobes on drops at beats 32 and 54.
  - Authored `tankerLightShow` (140 BPM, Dubstep/Industrial): hard amber/red snaps locked to wobble, blackout phrase gaps, red/amber strobe drops at beats 32 and 60.
  - Added `lightShowRegistry: Record<ShipType, LightCue[] | undefined>` with entries for `lng` and `tanker` only; all other ship types are `undefined` so they fall back to the existing generic beat-intensity curve.
  - Added `SHIP_BPM: Record<ShipType, number>` mirror of the BPM map used by `MusicSystem`.
  - Wired cue dispatch into `LightingSystem`:
    - `startHarborShow` now stores the ship's BPM from `SHIP_BPM`.
    - `update` resolves the active cue by current beat; if a cue table exists it computes pattern-specific pulse (`breathe`, `sweep`, `strobe`, `snap`, `blackout`) and sets intensity/color accordingly.
    - If no cue table exists (or show is inactive), the original generic sine-wave curve and intensity ramp are preserved.
    - Added `getCueState()` public accessor as the dispatch hook.
    - `getEmissiveColor` returns the active cue color when a cue is active, otherwise keeps the original rainbow HSL fallback.
- What remains (per-ship TODOs — do NOT stub empty files until ready to author):
  - [ ] `cruise` — Orchestral Pop Symphony (120 BPM) — warm sweeping chase patterns that crest with the choir.
  - [ ] `container` — Future Bass / Techno (128 BPM) — neon freight lines, glitch strobe on the drop.
  - [ ] `bulk` — Industrial Metal / Hard Rock (135 BPM) — orange/white blast flashes, hard cut blackouts.
  - [ ] `roro` — Synthwave / Driving Rock (125 BPM) — sunset magenta/gold sweeps, driving snap rhythm.
  - [ ] `research` — Ambient / Scientific (110 BPM) — sonar green pulses, slow blue sweeps, minimal strobe.
  - [ ] `droneship` — Space Ambient / Electronic (105 BPM) — deep purple breathing, orbital white strobes.
  - [ ] `ferry` — Reggae / Calypso Fusion (115 BPM) — warm island color waves, laid-back breathe pattern.
  - [ ] `trawler` — Sea Shanty / Folk (95 BPM) — warm lantern amber, gentle pulses, no harsh strobes.
  - [ ] `horizon` — Oceanic Ambient / Post-Rock (100 BPM) — deep sea cyan/indigo slow builds, cinematic sweep climax.
- Blockers: none
- Notes for next author:
  - The dispatch logic is fully generic; authoring a new ship only requires adding a `src/systems/lightShows/<shipType>.ts` file, exporting `<shipType>LightShow: LightCue[]`, and registering it in `src/systems/lightShows/index.ts`.
  - Keep beat numbers aligned with `SHIP_BPM` so cues land on musically sensible bars.
  - `lightingSystem.ts` was not modified in callers; `cinematicSystem.ts` and `UpgradeMenu.tsx` still call `startHarborShow(shipId, shipType)` and automatically pick up the new per-ship BPM lookup.

## kimi-cli swarm state — 2026-06-21T15:35:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **cruise** — warm gold/ivory breathe-to-sweep elegance, bright white sweep climax at beat 24.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **container**

## kimi-cli swarm state — 2026-06-21T15:37:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **container** — alternating neon magenta/cyan sweeps building into a glitchy white/magenta/cyan strobe drop at beats 24–27.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **bulk**

## kimi-cli swarm state — 2026-06-21T15:39:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **bulk** — rust-orange/steel-grey snaps with blackout phrase gaps and a bright metallic snap barrage at beats 24–30.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **roro**

## kimi-cli swarm state — 2026-06-21T15:40:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **roro** — retro magenta/cyan synthwave sweeps with backbeat snaps and a double white/magenta strobe climax at beats 24–26.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **research**

## kimi-cli swarm state — 2026-06-21T15:42:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **research** — sparse blue-green sonar breathes/sweeps with a single bright ping strobe at beat 18.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **droneship**

## kimi-cli swarm state — 2026-06-21T15:43:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **droneship** — very sparse cold white/violet breathing with a single satellite-ping strobe at beat 16 and a final blackout.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **ferry**

## kimi-cli swarm state — 2026-06-21T15:45:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **ferry** — warm yellow downbeat breathes, turquoise off-beat snaps for island skank, plus sweeps at beats 8 and 20.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **trawler**

## kimi-cli swarm state — 2026-06-21T15:47:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (continued)
- Ship completed: **trawler** — minimal amber/gold lantern breathing with a single gentle sweep at beat 20.
- Build/test status: **PASS** (`npm run build` green, 100/100 Vitest tests green).
- Next ship up: **horizon**

## kimi-cli swarm state — 2026-06-21T15:50:00+02:00
### Iteration 3 — Per-Ship LightCue Schedules (completed)
- Ship completed: **horizon** — deep blue/teal slow breathing builds to a wide cyan sweep climax at beat 24, then fades back into the dark.
- Build/test status: **PASS** (`npm run build` green, 146/146 Vitest tests green).
- Next ship up: **none — all 11 ship types now have authored LightCue schedules.**

### Summary of new schedules
- cruise — warm gold/ivory breathe-to-sweep elegance, white sweep climax at beat 24.
- container — alternating neon magenta/cyan sweeps, glitchy strobe drop at beats 24–27.
- bulk — rust-orange/steel-grey snaps with blackout rests, metallic snap barrage at beats 24–30.
- roro — magenta/cyan synthwave sweeps, backbeat snaps, double strobe climax at beats 24–26.
- research — sparse blue-green sonar breathes/sweeps, single ping strobe at beat 18.
- droneship — cold white/violet breathing, satellite-ping strobe at beat 16, final blackout.
- ferry — warm yellow breathes, turquoise off-beat snaps, island sweeps at beats 8 and 20.
- trawler — minimal amber/gold lantern breathing with one gentle sweep at beat 20.
- horizon — deep blue/teal slow build to wide cyan sweep at beat 24, then fade.

### Files changed
- Created `src/systems/lightShows/{cruise,container,bulk,roro,research,droneship,ferry,trawler,horizon}.ts`
- Updated `src/systems/lightShows/index.ts` to register all new schedules
- Updated `src/systems/__tests__/lightingSystem.test.ts` fallback test to use an unknown ship
- Created `src/systems/lightShows/lightShows.test.ts` with per-ship registry + cue invariants
- Fixed pre-existing `src/components/UpgradeMenu.tsx` duplicate declarations that blocked `npm run build`

## kimi-cli swarm state — 2026-06-28T18:27:55+02:00
### Iteration — Per-Ship Cinematic Cutaway Plans (completed)
- Files changed:
  - Created `src/systems/cutaways/container.ts`
  - Created `src/systems/cutaways/bulk.ts`
  - Created `src/systems/cutaways/lng.ts`
  - Created `src/systems/cutaways/roro.ts`
  - Created `src/systems/cutaways/research.ts`
  - Created `src/systems/cutaways/droneship.ts`
  - Created `src/systems/cutaways/ferry.ts`
  - Created `src/systems/cutaways/trawler.ts`
  - Created `src/systems/cutaways/horizon.ts`
  - Updated `src/systems/cutaways/index.ts` to import, register, and re-export all 9 new plans
  - Updated `src/systems/cutaways/cutaways.test.ts` to assert every ShipType resolves to a non-default authored plan and to validate all 11 authored plans
- Build: PASS (`npx tsc --noEmit` clean)
- Tests: PASS (`npx vitest run` — 172/172 tests green, 26 cutaway tests green)
- Lint: PASS (`npx eslint src/systems/cutaways` — 0 errors, 0 warnings)
- What remains: Nothing — task complete.
- Blockers: none

### Shot-design rationale per ship
- **container** (128 BPM Future Bass / Techno) — rapid 2–3 beat cuts alternating `ship-low` and `ship-rig`, mirroring the neon magenta/cyan glitch-strobe drop in the light show.
- **bulk** (135 BPM Industrial Metal / Hard Rock) — aggressive 2-beat hammer blows between `ship-low` and `ship-rig`, matching the rust/steel snap blackouts.
- **lng** (118 BPM Ambient / Cryogenic Techno) — slow 6–8 beat holds on `ship-aerial` and `ship-water`, breathing with the frost-blue cue schedule.
- **roro** (125 BPM Synthwave / Driving Rock) — 3–4 beat driving cuts across `ship-low`, `ship-aerial`, `ship-rig`, and `ship-water` for retro highway momentum.
- **research** (110 BPM Ambient / Scientific) — observational 6–8 beat holds on `ship-aerial`/`ship-water`, closing with a brief `ship-rig` before climax.
- **droneship** (105 BPM Space Ambient / Electronic) — long 7–8 beat orbital holds on `ship-aerial`/`ship-rig`, sparse and cosmic like the satellite-ping light cue.
- **ferry** (115 BPM Reggae / Calypso Fusion) — relaxed 4–6 beat groove holds mixing `ship-water`, `ship-low`, and `ship-aerial`, matching warm island skank timing.
- **trawler** (95 BPM Sea Shanty / Folk) — the slowest pacing: 8-beat rolling holds on `ship-water`/`ship-aerial`, lantern warmth, one tight `ship-rig` at beat 22.
- **horizon** (100 BPM Oceanic Ambient / Post-Rock) — vast 7–8 beat swells from `ship-aerial` to `ship-water`, ending on `ship-low` for the wide sweep climax.

### Verification notes
- Every `ShipType` now resolves to its own non-default `CutawayPlan` (`getCutawayPlan(shipType) !== DEFAULT_CUTAWAY_PLAN`).
- All plans stay within the 0..32 beat envelope, place `spotlight_pulse` at beat 4, `climax` at beat 24, `spectator_drone` shortly after, and `hide_band_name` at beat 32.
- No new CameraMode values, CutawayAction variants, store actions, or changes to lightShows/ components/ scenes/ store slices were made.

## kimi-cli swarm state — 2026-07-05T18:24:00+02:00
### Iteration 0 — Wildlife & Scenic Coastal Ecosystem, Phase 1 (Foundation)
- Task: add an ambient, season/time/weather-aware marine-life layer without disturbing the existing event-driven wildlife.
- Architecture decision recorded **before** making changes:
  - Existing event-driven wildlife (`src/systems/wildlifeSystem.ts`, `src/scenes/Wildlife.tsx`) handles discrete megafauna: `humpback_whale`, `great_white_shark`, `bottlenose_dolphin`, `bioluminescent_plankton`. It is timer-based (30 s), capped at 20 entities, and driven by `seaEventsSystem.ts` / `HarborEventSystem.ts` / `dynamicEventSystem.ts`.
  - Phase 1 targets are aggregations/particle layers by nature (moon jellyfish blooms, fish schools, night plankton glow), so a **separate ambient layer** is cleaner than forcing them into the `WildlifeType` union.
  - Plan: add `season`, `wildlifeDensity`, `enableMarineLife` to the store; create `src/systems/wildlifeProfiles.ts` for data + pure helpers; create `src/systems/ambientMarineLifeSystem.ts` + `src/scenes/AmbientMarineLife.tsx` as the new instanced ambient layer; wire into `MainScene.tsx`; add Leva "Marine Life" folder.
- Decision rationale: keeps existing event-driven spawn path untouched, avoids fighting the 20-entity cap with schools/particle fields, and maps cleanly to instanced/particle rendering.
- Source of truth for multipliers: `weekly_plan.md` §"Wildlife & Scenic Coastal Ecosystem".
- Status: plan approved; implementation starting.

## kimi-cli swarm state — 2026-07-05T18:45:00+02:00
### Iteration 1 — Wildlife & Scenic Coastal Ecosystem, Phase 1 (Foundation) COMPLETE
- Files changed:
  - `src/store/gameStoreTypes.ts` — added `Season` type and `season`/`wildlifeDensity`/`enableMarineLife` fields + setters + defaults + persistence.
  - `src/store/slices/slice1.ts` — implemented `setSeason`, `setWildlifeDensity`, `setEnableMarineLife`; wired save/load/reset.
  - `src/utils/storage_manager.ts` — added new fields to `GameState` type.
  - `src/systems/wildlifeProfiles.ts` (new) — `WILDLIFE_PROFILES` data table (moon jellyfish, fish schools, night plankton) with season/time/weather multipliers and pure helpers `getTimeOfDay`, `getAmbientDensity`, `getAmbientCounts`.
  - `src/systems/__tests__/wildlifeProfiles.test.ts` (new) — 13 tests covering time-of-day, seasonality, weather, storm reduction, quality LOD scaling, clamping.
  - `src/systems/ambientMarineLifeSystem.ts` (new) — singleton spawner that reads store fields and reconciles transient ambient creature arrays.
  - `src/scenes/AmbientMarineLife.tsx` (new) — instanced renderer for moon jellies + fish schools + points-based night plankton glow; uses `useMusicPulse` for subtle bioluminescence pulse.
  - `src/scenes/MainScene.tsx` — calls `ambientMarineLifeSystem.update(delta)` each frame and renders `<AmbientMarineLife />` after `<WildlifeRenderer />`.
  - `src/scenes/mainScene/MainSceneHelpers.tsx` — extended `LevaControlsConfig` and added "Marine Life" folder with `Enable Marine Life`, `Wildlife Density`, and `Season` controls.
- Build: PASS (`npx tsc --noEmit` 0 errors).
- Tests: PASS (`npx vitest run` — 185/185 tests green, up from 172 with the 13 new wildlifeProfiles tests).
- Lint: no new errors (9 pre-existing warnings in `MainSceneHelpers.tsx` only).
- What remains: Nothing — Phase 1 foundation complete.
- Blockers: none.
- Decision recorded: separate ambient layer (not extending `WildlifeType` union) because Phase 1 species are aggregations/particle fields, not discrete event-driven megafauna. Existing whale/shark/dolphin/plankton event path remains untouched.
- Next step: Phase 2 — whale/seabird ambient passes (Gray Whale surface passes + seabird flocks, plus camera-aware spawn bias for spectator-drone shots).

## kimi-cli swarm state — 2026-07-05T19:40:00+02:00
### Iteration 2 — Wildlife & Scenic Coastal Ecosystem, Phase 2 (Charismatic Megafauna + Camera Bias) COMPLETE
- Files changed:
  - `src/systems/wildlifeProfiles.ts` — added `gray_whale` and `seabird_flock` ambient species with season/time/weather profiles; added `getCameraAwareMultiplier()` and wired camera mode + focused viewport into `getAmbientCounts()`.
  - `src/systems/ambientMarineLifeSystem.ts` — extended creature arrays and spawn logic for gray whales + seabird flocks; added whale cruise + seabird soaring motion.
  - `src/scenes/AmbientMarineLife.tsx` — added instanced renderers for gray whales (capsule) and seabird flocks (cone), refactored shared instanced logic into `GenericInstancedSpecies`.
  - `src/systems/__tests__/wildlifeProfiles.test.ts` — expanded from 13 to 21 tests covering gray whale seasonality, seabird day/night behavior, camera-aware multipliers, and underwater-viewport bias.
- Build: PASS (`npx tsc --noEmit` 0 errors).
- Tests: PASS (`npx vitest run` — 193/193 tests green, up from 185 with 8 new Phase 2 tests).
- Lint: no new errors in changed files.
- What remains: Nothing — Phase 2 complete.
- Blockers: none.
- Next step: Phase 3 — camera-aware spawning (shift spawn origins toward active scenic camera frustum), seasonal visual variants (jelly colors, whale barnacle coverage), and optional light-rig attraction.

## kimi-cli swarm state — 2026-07-05T22:05:00+02:00
### Iteration 3 — Wildlife & Scenic Coastal Ecosystem, Phase 3 (Camera-Frustum Spawning + Seasonal Variants + Light Attraction) COMPLETE
- Files changed:
  - `src/systems/wildlifeProfiles.ts` — added `getSeasonalColor()` helper with per-species/season tints; added `getLightAttractionMultiplier()` that boosts ambient density when any ship is fully upgraded and its music/light show is playing.
  - `src/systems/ambientMarineLifeSystem.ts` — `update(delta, camera?)` now accepts the active R3F camera; when in scenic modes (`spectator`/`ship-water`/`ship-aerial` or focused underwater viewport), new ambient creatures spawn in a region centered on the camera's view frustum instead of the full harbor bounds; applies the light-rig attraction boost to target counts.
  - `src/scenes/AmbientMarineLife.tsx` — applies `getSeasonalColor()` per instance via `setColorAt()` (with slight individual variation); plankton points material now shifts color by season; refactored props to pass `season` down to all instanced species.
  - `src/scenes/MainScene.tsx` — passes the active `camera` from `useThree()` into `ambientMarineLifeSystem.update(delta, camera)`.
  - `src/systems/__tests__/wildlifeProfiles.test.ts` — expanded from 21 to 26 tests covering seasonal colors and light-rig attraction multiplier.
- Build: PASS (`npx tsc --noEmit` 0 errors).
- Tests: PASS (`npx vitest run` — 198/198 tests green, up from 193 with 5 new Phase 3 tests).
- Lint: no new errors in changed files.
- What remains: Nothing — Phase 3 complete.
- Blockers: none.
- Next step: Phase 4 — expanded coastal flora / kelp beds, interaction hooks (crane movement or bright lights cause temporary wildlife reactions), and deeper camera-frustum culling/LOD.

## kimi-cli swarm state — 2026-07-05T22:10:00+02:00
### Iteration 4 — Wildlife & Scenic Coastal Ecosystem, Phase 4 (Kelp Beds + Interaction Hooks + LOD) COMPLETE
- Files changed:
  - `src/systems/wildlifeProfiles.ts` — added `kelp_bed` ambient species with seasonal shore-side density profile and seasonal color tints (green → brown).
  - `src/systems/ambientMarineLifeSystem.ts` — added kelp arrays; kelp spawns in a shore-side region (z 10–50, x ±70); added crane-spreader disturbance hook (`disturbance` + `driftVelocity` on each creature) so fish/jellies/plankton/kelp flee and flash brighter when the spreader gets close; added distance-based LOD multiplier that reduces ambient counts when the camera is far from the harbor center.
  - `src/scenes/AmbientMarineLife.tsx` — added `KelpBedInstanced` renderer (plane geometry, tall swaying kelp); per-instance color now brightens with `creature.disturbance`.
  - `src/systems/__tests__/wildlifeProfiles.test.ts` — expanded from 26 to 28 tests covering kelp seasonal colors and kelp daytime spawning.
- Build: PASS (`npx tsc --noEmit` 0 errors).
- Tests: PASS (`npx vitest run` — 200/200 tests green, up from 198 with 2 new Phase 4 tests).
- Lint: no new errors in changed files.
- What remains: Nothing — Phase 4 complete. The ambient marine-life pillar is now feature-complete through Phase 4.
- Blockers: none.
- Next step: Polish / optimization pass (e.g., move spawn-center math to a pure helper for testability, add distance-based material fade, or author a dedicated kelp shader). Alternatively, close the Wildlife & Scenic Coastal Ecosystem Idea in `weekly_plan.md`.
