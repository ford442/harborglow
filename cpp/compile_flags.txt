# clangd / clang-tidy fallback when compile_commands.json is absent.
# Matches the em++ reactor so DSP_EXPORT and SIMD intrinsic macros resolve.
# Native objects use -std=c++17 / -std=c11; see Makefile and compile_commands.
--target=wasm32-unknown-emscripten
-std=c++17
-I.
-msimd128
-matomics
-mbulk-memory
-Wall
-Wextra
-Wshadow
-Wconversion
-Werror=return-type
-DDSP_EXPORT=
-DEMSCRIPTEN_KEEPALIVE=


# Generate a full database (one entry per TU): make -C cpp compile-commands
