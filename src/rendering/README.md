# Rendering / Renderer Backends

Internal documentation for the dual-renderer system (WebGPU + WebGL2 fallback).

See the public-facing `docs/RENDERER.md` at the repo root for usage instructions aimed at developers and agents.