/**
 * Archived GLSL for the Gerstner ocean (WebGL-era ShaderMaterial).
 * Live shading is TSL in gerstnerTsl.ts — this file is reference only.
 */
export const WATER_GERSTNER_GLSL_REFERENCE = /* glsl */ `
  // getWaveHeight / getWaveNormal / getFoam / getRippleDetail / caustics
  // and the former vertex+fragment ShaderMaterial live in git history.
  // Do not construct THREE.ShaderMaterial from this string.
`
